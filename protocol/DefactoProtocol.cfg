SPECIFICATION Spec

\* Type sanity + protocol invariants
INVARIANT TypeOK
INVARIANT NoStaleIdentity
INVARIANT NoStaleEventEntities
INVARIANT MergeAuditComplete
INVARIANT DashmapOwnership

\* Model parameters
CONSTANT NumShards = 2
CONSTANT MaxEvents = 3

\* No deadlock check — system can reach states where only
\* stuttering is possible (e.g., all events ingested, all cursors caught up).
CHECK_DEADLOCK FALSE
